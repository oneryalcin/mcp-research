{
  "mcpServers": {
    "health": {
      "type": "stdio",
      "command": "uv",
      "args": ["run", "--quiet", "--isolated", "--with", "fastmcp[code-mode]", "python", "REPLACE_WITH_ABSOLUTE_PATH/server.py"],
      "env": {"CODEMODE_MODE": "native"}
    }
  }
}
